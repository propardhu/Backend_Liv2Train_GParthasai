apply plugin: "war"

bootWar {
    mainClassName = "com.liv2train.app.BackendLiv2TrainGParthasaiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
